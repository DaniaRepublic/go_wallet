package main

// Apple wallet urls
var registerURL = "/v1/devices/:deviceLibraryIdentifier/registrations/:passTypeIdentifier/:serialNumber"
var updatablepassesURL = "/v1/devices/:deviceLibraryIdentifier/registrations/:passTypeIdentifier"
var sendupdatedpassURL = "/v1/passes/:passTypeIdentifier/:serialNumber"

// Website urls
var loginURL = "/login"
var passesURL = "/passes"
var updateURL = "/update"
var commitURL = "/commit"
